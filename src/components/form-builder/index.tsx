import React from "react";

export type FormBuilderProps = {
  onSave?: (formId: string) => void;
};

export function FormBuilder(_: FormBuilderProps) {
  // Minimal stub to satisfy imports after removing full form-builder implementation
  return null;
}

export default FormBuilder;
